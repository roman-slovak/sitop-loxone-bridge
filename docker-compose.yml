services:
  sitop-bridge:
    build: .
    image: sitop-loxone-bridge:latest
    container_name: sitop-loxone-bridge
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
